'use client';

// ClickTracker simplificado - agora os clicks são registrados pelo middleware
// Este componente serve apenas para manter a estrutura do layout

export default function ClickTracker({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
